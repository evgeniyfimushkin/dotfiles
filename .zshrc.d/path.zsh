export PATH="${KREW_ROOT:-$HOME/.krew}/bin:$PATH"

# The next line updates PATH for CLI.
if [ -f '/Users/evgeniyfim/yandex-cloud/path.bash.inc' ]; then source '/Users/evgeniyfim/yandex-cloud/path.bash.inc'; fi

# The next line enables shell command completion for yc.
if [ -f '/Users/evgeniyfim/yandex-cloud/completion.zsh.inc' ]; then source '/Users/evgeniyfim/yandex-cloud/completion.zsh.inc'; fi
