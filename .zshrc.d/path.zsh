export PATH=$PATH:~/.local/bin
export PATH=$PATH:~/scripts
# Only set KUBECONFIG if kube config files exist
# if ls ~/.kube/*.yaml &>/dev/null 2>&1; then
#     export KUBECONFIG=$(ls ~/.kube/*.yaml | tr '\n' ':')
# fi
